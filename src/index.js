/**
 * image-crop-engine
 * 
 * A utility for processing and manipulating image data for cropping operations.
 * Platform-agnostic module designed to work in any JavaScript environment.
 */

const ImageCropEngine = require('./image-crop-engine');

module.exports = {
  ImageCropEngine
}; 